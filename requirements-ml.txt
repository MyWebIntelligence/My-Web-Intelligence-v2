# Optional ML extras (install in addition to requirements.txt)
# Enables ANN + NLI (FAISS + transformers/sentencepiece/torch)
#
# Source of truth: [project.optional-dependencies].ml in pyproject.toml.
# For a reproducible, lock-backed install prefer:  uv sync --extra ml

torch>=2.2
transformers>=4.40
tokenizers>=0.15
sentencepiece>=0.1.99
sentence-transformers>=2.6
faiss-cpu>=1.12.0

